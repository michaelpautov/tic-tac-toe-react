import styled from 'styled-components';

export const WinnerWrapper = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  display: flex;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.3);
  justify-content: center;
  align-items: center;
`;

export const WinnerPopup = styled.div`
  padding: 20px;
  font-family: 'Comic Sans MS', cursive, sans-serif;
  font-size: 36px;
  background: rgba(0, 255, 237, 0.9);
`;
